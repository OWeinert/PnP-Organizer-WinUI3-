namespace PnPOrganizer.Core.Character.SkillSystem
{
    /// <summary>
    /// Defines when in the calculation a Skill's bonus is added/multiplied to the current value
    /// </summary>
    public enum ApplianceMode
    {
        BaseValue,
        EndValue
    }
}
